## React: Output Dynamic Values ##

This code snapshot is an example of a React Component which outputs both dynamic and static texts (values)

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`
