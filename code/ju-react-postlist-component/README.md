## React: Postlist Component ##

This code snapshot is an example of how we can simply shows PostList by creating a second component.
S

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`
