## React: Submit and output list of Posts ##

This code snapshot is an example of how we can insert and shows list of posts

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`