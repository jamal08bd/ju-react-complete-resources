## React: State ##

This code snapshot is an example of how we use React State with Event to interact with changed output.

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`
